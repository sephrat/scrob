---
import Base from "../layouts/Base.astro";
import { api } from "../lib/api";
import LanguagePicker from "../components/LanguagePicker.astro";
import { m } from "../paraglide/messages.js";

let submitted = false;
let error = "";

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const email = data.get("email")?.toString() ?? "";
    await api.auth.forgotPassword(email);
    submitted = true;
  } catch (e: unknown) {
    error = e instanceof Error ? e.message : m.common_something_went_wrong_retry();
  }
}
---

<Base title={m.forgot_page_title()} hideNav>
  <div class="max-w-md mx-auto mt-16 p-8 bg-zinc-900 rounded-xl border border-zinc-800 shadow-2xl relative">
    <button
      id="theme-toggle"
      class="absolute top-4 right-4 p-2 rounded-lg bg-zinc-800 border border-zinc-700 text-zinc-400 hover:text-zinc-100 transition-all hover:scale-110 active:scale-95"
      aria-label={m.nav_toggle_theme()}
    >
      <span id="sun-icon" class="hidden">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/></svg>
      </span>
      <span id="moon-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/></svg>
      </span>
    </button>

    <div class="flex flex-col items-center mb-6">
      <img src="/scrob.png" alt={m.nav_logo_alt()} class="w-36 object-contain mb-2" />
      <span class="text-zinc-100 font-bold text-xl tracking-tight">Scrob</span>
    </div>

    <h1 class={`text-3xl font-bold mb-6 text-zinc-100 ${submitted ? "text-center" : ""}`}>
      {submitted ? m.register_check_inbox() : m.forgot_heading()}
    </h1>

    {error && (
      <div class="bg-red-500/10 border border-red-500 text-red-500 p-3 rounded-lg mb-6">
        {error}
      </div>
    )}

    {submitted ? (
      <div class="text-center py-2">
        <div class="w-14 h-14 bg-zinc-800 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-7 h-7 text-zinc-300">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
          </svg>
        </div>
        <p class="text-sm text-zinc-400 mb-6" set:html={m.forgot_sent_body()} />
        <a href="/login" class="text-sm text-zinc-400 hover:text-zinc-100 transition-colors">{m.auth_back_to_login()}</a>
      </div>
    ) : (
      <form method="POST" class="space-y-4">
        <div>
          <label for="email" class="block text-sm font-medium text-zinc-400 mb-1">{m.auth_email()}</label>
          <input
            type="email"
            name="email"
            id="email"
            required
            autofocus
            class="w-full bg-zinc-800 border border-zinc-700 rounded-lg px-4 py-3 text-zinc-100 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-zinc-100 text-zinc-950 font-bold py-2 rounded-lg hover:bg-zinc-200 transition-colors cursor-pointer mt-4"
        >
          {m.forgot_send_link()}
        </button>
      </form>
    )}

    <div class="mt-6 flex justify-center">
      <LanguagePicker />
    </div>

    {!submitted && (
      <div class="mt-6 text-center text-sm text-zinc-500">
        <a href="/login" class="text-zinc-400 hover:text-zinc-100 transition-colors">{m.auth_back_to_login()}</a>
      </div>
    )}
  </div>
</Base>
